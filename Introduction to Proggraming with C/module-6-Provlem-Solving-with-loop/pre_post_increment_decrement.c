#include <stdio.h>
// This program demonstrates the difference between pre-increment and post-increment operators in C.

// # Post-increment (x++): The current value of x is used in the expression, and then x is incremented by 1.


// # Pre-increment (++x): x is incremented by 1 first, and then the new value of x is used in the expression.


int main(){

    // int x = 5;
    // int y = x++; // Post-increment: y gets the value of x, then x is incremented
    // printf("Post-increment:\n");
    // printf("x: %d, y: %d\n", x, y); // x is now 6, y is 5




    // int a = 5;
    // int b = ++a; // Pre-increment: a is incremented first, then b gets the value of a
    // printf("Pre-increment:\n");
    // printf("a: %d, b: %d\n", a, b); // a is now 6, b is 6
    // return 0;
}